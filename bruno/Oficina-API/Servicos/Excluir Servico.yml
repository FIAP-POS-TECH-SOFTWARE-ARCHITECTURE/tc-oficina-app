info:
  name: Excluir Servico
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{url}}/servicos/{{servicoId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
