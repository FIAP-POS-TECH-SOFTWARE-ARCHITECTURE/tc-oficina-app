info:
  name: Excluir Servico
  type: http
  seq: 1

http:
  method: DELETE
  url: "{{url}}/servicos/{{servicoId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
