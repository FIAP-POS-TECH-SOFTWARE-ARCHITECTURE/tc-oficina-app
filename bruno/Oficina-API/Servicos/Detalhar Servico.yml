info:
  name: Detalhar Servico
  type: http
  seq: 3

http:
  method: GET
  url: "{{url}}/servicos/{{servicoId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
