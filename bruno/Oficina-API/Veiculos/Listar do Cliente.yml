info:
  name: Listar do Cliente
  type: http
  seq: 2

http:
  method: GET
  url: "{{url}}/clientes/{{clienteId}}/veiculos"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

