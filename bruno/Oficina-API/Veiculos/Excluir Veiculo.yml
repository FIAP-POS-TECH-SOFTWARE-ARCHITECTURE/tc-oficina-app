info:
  name: Excluir Veiculo
  type: http
  seq: 1

http:
  method: DELETE
  url: "{{url}}/veiculos/{{veiculoId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
