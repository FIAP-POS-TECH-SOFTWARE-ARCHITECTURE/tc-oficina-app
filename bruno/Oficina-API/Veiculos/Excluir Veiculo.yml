info:
  name: Excluir Veiculo
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{url}}/veiculos/{{veiculoId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
