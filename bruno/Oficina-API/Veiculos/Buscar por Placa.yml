info:
  name: Buscar por Placa
  type: http
  seq: 3

http:
  method: GET
  url: "{{url}}/veiculos/buscar?placa=ABC1D23"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

