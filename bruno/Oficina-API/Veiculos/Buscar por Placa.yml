info:
  name: Buscar por Placa
  type: http
  seq: 4

http:
  method: GET
  url: "{{url}}/veiculos/buscar?placa={{placa}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
