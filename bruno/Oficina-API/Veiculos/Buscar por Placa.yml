info:
  name: Buscar por Placa
  type: http
  seq: 4

http:
  method: GET
  url: "{{url}}/veiculos/buscar?placa={{placa}}"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

