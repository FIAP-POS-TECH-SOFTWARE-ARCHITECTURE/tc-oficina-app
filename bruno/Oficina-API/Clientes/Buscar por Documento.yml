info:
  name: Buscar por Documento
  type: http
  seq: 4

http:
  method: GET
  url: "{{url}}/clientes/buscar?documento={{documentoCliente}}"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

