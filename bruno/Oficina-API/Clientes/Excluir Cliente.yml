info:
  name: Excluir Cliente
  type: http
  seq: 1

http:
  method: DELETE
  url: "{{url}}/clientes/{{clienteId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
