info:
  name: Excluir Cliente
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{url}}/clientes/{{clienteId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
