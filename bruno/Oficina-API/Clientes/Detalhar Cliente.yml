info:
  name: Detalhar Cliente
  type: http
  seq: 3

http:
  method: GET
  url: "{{url}}/clientes/{{clienteId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
