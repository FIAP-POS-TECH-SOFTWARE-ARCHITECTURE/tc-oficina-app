info:
  name: Detalhar Usuario
  type: http
  seq: 1

http:
  method: GET
  url: "{{url}}/usuarios/{{usuarioId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
