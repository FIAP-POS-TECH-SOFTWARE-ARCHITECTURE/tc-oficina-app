info:
  name: Excluir Usuario
  type: http
  seq: 1

http:
  method: DELETE
  url: "{{url}}/usuarios/{{usuarioId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
