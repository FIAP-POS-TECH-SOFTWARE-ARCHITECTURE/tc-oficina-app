info:
  name: Listar Usuarios
  type: http
  seq: 2

http:
  method: GET
  url: "{{url}}/usuarios"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

