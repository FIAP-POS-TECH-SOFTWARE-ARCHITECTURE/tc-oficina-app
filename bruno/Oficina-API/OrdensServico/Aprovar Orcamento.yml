info:
  name: Aprovar Orcamento
  type: http
  seq: 9

http:
  method: POST
  url: "{{url}}/os/{{osNumero}}/orcamento/aprovar"
  auth: none
  body:
    type: json
    data: |-
      {
          "observacao": "Pode iniciar o serviço"
        }

headers:
  - name: Authorization
    value: "Bearer {{clienteToken}}"
    enabled: true

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
