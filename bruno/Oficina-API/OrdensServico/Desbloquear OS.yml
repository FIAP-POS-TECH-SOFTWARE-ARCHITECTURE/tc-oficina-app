info:
  name: Desbloquear OS
  type: http
  seq: 10

http:
  method: POST
  url: "{{url}}/os/{{osId}}/desbloquear"
  body:
    type: json
    data: |-
      {
          "observacao": "Estoque reposto, OS liberada para execução"
        }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
