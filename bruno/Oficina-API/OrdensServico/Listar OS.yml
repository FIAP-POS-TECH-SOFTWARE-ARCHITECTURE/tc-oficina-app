info:
  name: Listar OS
  type: http
  seq: 19

http:
  method: GET
  url: "{{url}}/os?page=1&pageSize=20"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

