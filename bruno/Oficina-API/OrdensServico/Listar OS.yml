info:
  name: Listar OS
  type: http
  seq: 15

http:
  method: GET
  url: "{{url}}/os?page=1&pageSize=20"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

