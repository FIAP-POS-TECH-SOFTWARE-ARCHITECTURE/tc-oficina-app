info:
  name: Iniciar Item Servico
  type: http
  seq: 12

http:
  method: POST
  url: "{{url}}/os/{{osId}}/itens-servico/{{itemServicoId}}/iniciar"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

