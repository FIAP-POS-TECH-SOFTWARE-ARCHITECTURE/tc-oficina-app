info:
  name: Finalizar
  type: http
  seq: 15

http:
  method: POST
  url: "{{url}}/os/{{osId}}/finalizar"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
