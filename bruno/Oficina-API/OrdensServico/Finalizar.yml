info:
  name: Finalizar
  type: http
  seq: 11

http:
  method: POST
  url: "{{url}}/os/{{osId}}/finalizar"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

