info:
  name: Metrica Tempo Medio
  type: http
  seq: 14

http:
  method: GET
  url: "{{url}}/os/metricas/tempo-medio"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

