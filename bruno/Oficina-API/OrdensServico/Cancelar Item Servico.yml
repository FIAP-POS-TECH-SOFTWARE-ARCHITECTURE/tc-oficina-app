info:
  name: Cancelar Item Servico
  type: http
  seq: 14

http:
  method: POST
  url: "{{url}}/os/{{osId}}/itens-servico/{{itemServicoId}}/cancelar"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
