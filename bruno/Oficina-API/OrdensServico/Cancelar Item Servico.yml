info:
  name: Cancelar Item Servico
  type: http
  seq: 13

http:
  method: POST
  url: "{{url}}/os/{{osId}}/itens-servico/{{itemServicoId}}/cancelar"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

