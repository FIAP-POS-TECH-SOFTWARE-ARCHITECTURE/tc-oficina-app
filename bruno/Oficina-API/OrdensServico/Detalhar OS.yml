info:
  name: Detalhar OS
  type: http
  seq: 14

http:
  method: GET
  url: "{{url}}/os/{{osId}}"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

