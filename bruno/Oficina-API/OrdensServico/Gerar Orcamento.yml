info:
  name: Gerar Orcamento
  type: http
  seq: 5

http:
  method: POST
  url: "{{url}}/os/{{osId}}/orcamento/gerar"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

