info:
  name: Consulta Publica
  type: http
  seq: 19

http:
  method: GET
  url: "{{url}}/os/publica/{{osNumero}}?documento={{documentoCliente}}"
  auth: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
