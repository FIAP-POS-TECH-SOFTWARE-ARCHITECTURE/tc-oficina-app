info:
  name: Cancelar
  type: http
  seq: 17

http:
  method: POST
  url: "{{url}}/os/{{osId}}/cancelar"
  body:
    type: json
    data: |-
      {
          "motivo": "Cancelamento administrativo"
        }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
