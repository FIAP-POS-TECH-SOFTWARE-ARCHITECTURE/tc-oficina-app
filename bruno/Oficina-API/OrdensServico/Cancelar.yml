info:
  name: Cancelar
  type: http
  seq: 13

http:
  method: POST
  url: "{{url}}/os/{{osId}}/cancelar"
  body:
    type: json
    data: |-
      {
          "motivo": "Cancelamento administrativo"
        }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

