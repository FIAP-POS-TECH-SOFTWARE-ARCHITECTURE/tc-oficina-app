info:
  name: Remover Item Servico
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{url}}/os/{{osId}}/itens-servico/{{itemServicoId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
