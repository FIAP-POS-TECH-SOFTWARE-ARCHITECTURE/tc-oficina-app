info:
  name: Rejeitar Orcamento
  type: http
  seq: 11

http:
  method: POST
  url: "{{url}}/os/{{osNumero}}/orcamento/rejeitar"
  body:
    type: json
    data: |-
      {
          "documento": "{{documentoCliente}}",
          "observacao": "Cliente desistiu do reparo"
        }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
