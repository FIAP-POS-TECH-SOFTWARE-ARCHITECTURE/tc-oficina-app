info:
  name: Iniciar Diagnostico
  type: http
  seq: 2

http:
  method: POST
  url: "{{url}}/os/{{osId}}/diagnostico/iniciar"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
