info:
  name: Entregar
  type: http
  seq: 16

http:
  method: POST
  url: "{{url}}/os/{{osId}}/entregar"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

