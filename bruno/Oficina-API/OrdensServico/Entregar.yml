info:
  name: Entregar
  type: http
  seq: 12

http:
  method: POST
  url: "{{url}}/os/{{osId}}/entregar"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

