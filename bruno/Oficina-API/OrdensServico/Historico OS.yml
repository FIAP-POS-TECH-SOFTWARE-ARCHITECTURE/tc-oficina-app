info:
  name: Historico OS
  type: http
  seq: 22

http:
  method: GET
  url: "{{url}}/os/{{osId}}/historico"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
