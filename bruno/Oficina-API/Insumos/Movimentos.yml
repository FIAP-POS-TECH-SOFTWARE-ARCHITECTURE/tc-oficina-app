info:
  name: Movimentos
  type: http
  seq: 7

http:
  method: GET
  url: "{{url}}/insumos/{{insumoId}}/movimentos"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

