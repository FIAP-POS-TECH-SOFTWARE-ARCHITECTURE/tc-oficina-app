info:
  name: Movimentos
  type: http
  seq: 7

http:
  method: GET
  url: "{{url}}/insumos/{{insumoId}}/movimentos"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
