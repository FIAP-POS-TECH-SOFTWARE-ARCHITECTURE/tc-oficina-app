info:
  name: Excluir Insumo
  type: http
  seq: 9

http:
  method: DELETE
  url: "{{url}}/insumos/{{insumoId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
