info:
  name: Detalhar Insumo
  type: http
  seq: 1

http:
  method: GET
  url: "{{url}}/insumos/{{insumoId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
