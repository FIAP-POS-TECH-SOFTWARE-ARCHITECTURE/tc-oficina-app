info:
  name: Cancelar Registro de Compra
  type: http
  seq: 5

http:
  method: POST
  url: "{{url}}/insumos/compras/{{registroCompraId}}/cancelar"
  body:
    type: json
    data: |-
      {
          "motivo": "Insumo não mais necessário"
        }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
