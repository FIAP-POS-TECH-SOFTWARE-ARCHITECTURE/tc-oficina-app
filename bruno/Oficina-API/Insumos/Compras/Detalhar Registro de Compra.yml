info:
  name: Detalhar Registro de Compra
  type: http
  seq: 7

http:
  method: GET
  url: "{{url}}/insumos/compras/{{registroCompraId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
