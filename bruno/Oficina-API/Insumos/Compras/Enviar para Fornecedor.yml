info:
  name: Enviar para Fornecedor
  type: http
  seq: 2

http:
  method: POST
  url: "{{url}}/insumos/compras/{{registroCompraId}}/enviar-fornecedor"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
