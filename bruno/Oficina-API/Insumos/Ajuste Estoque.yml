info:
  name: Ajuste Estoque
  type: http
  seq: 1

http:
  method: POST
  url: "{{url}}/insumos/{{insumoId}}/ajuste"
  body:
    type: json
    data: |-
      {
          "novaQuantidade": 12,
          "motivo": "Contagem de inventario"
        }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
