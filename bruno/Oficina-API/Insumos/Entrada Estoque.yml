info:
  name: Entrada Estoque
  type: http
  seq: 3

http:
  method: POST
  url: "{{url}}/insumos/{{insumoId}}/entrada"
  body:
    type: json
    data: |-
      {
          "quantidade": 5,
          "motivo": "Compra fornecedor"
        }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

