info:
  name: Entrada Estoque
  type: http
  seq: 5

http:
  method: POST
  url: "{{url}}/insumos/{{insumoId}}/entrada"
  body:
    type: json
    data: |-
      {
          "quantidade": 5,
          "motivo": "Compra fornecedor"
        }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
